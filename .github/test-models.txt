# Pulled by .github/scripts/pull-models.sh. One `<backend> <model-id>` per line,
# backend `kronk` or `bucky`; blank lines and `#` comments are ignored.
#
# ~6.6 GiB uncompressed. Fork PRs re-pull the whole list on ubuntu-latest every
# run, so anything bigger belongs in .github/test-models-gpu.txt.

kronk unsloth/Qwen3.5-0.8B-Q8_0
kronk unsloth/Qwen3-1.7B-Q4_K_M
kronk unsloth/Qwen3-0.6B-Q8_0
kronk mradermacher/Qwopus3.5-4B-Coder.Q4_K_M
kronk nomic-ai/nomic-embed-text-v1.5.Q8_0
kronk gpustack/bge-reranker-v2-m3-Q8_0
bucky ggml-tiny.bin
