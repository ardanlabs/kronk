# Models pulled ONLY by gpu.yml, on top of .github/test-models.txt. Same format.
#
# Separate because linux.yml falls back to ubuntu-latest for fork PRs, where
# these multi-GB targets do not fit; the GPU fleet keeps them between jobs.

# The two speculative-decoding MTP targets, the only GPU coverage of that path
# (sdk/kronk/tests/mtp; gemma-4 shares the target's KV, sdk/kronk/tests/gemma4mtp).
kronk unsloth/mtp-Qwen3.6-35B-A3B-UD-Q2_K_XL
kronk unsloth/gemma-4-26B-A4B-it-UD-Q4_K_M
